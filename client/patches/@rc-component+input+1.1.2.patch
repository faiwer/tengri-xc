diff --git a/node_modules/@rc-component/input/es/utils/commonUtils.js b/node_modules/@rc-component/input/es/utils/commonUtils.js
index e012c88..28183c2 100644
--- a/node_modules/@rc-component/input/es/utils/commonUtils.js
+++ b/node_modules/@rc-component/input/es/utils/commonUtils.js
@@ -5,23 +5,21 @@ export function hasPrefixSuffix(props) {
   return !!(props.prefix || props.suffix || props.allowClear);
 }
 
-// TODO: It's better to use `Proxy` replace the `element.value`. But we still need support IE11.
+// Patched (tengri-xc): the upstream `Object.create(event, { target, currentTarget })`
+// breaks on real DOM events. Reading any Event.prototype accessor (`.type`,
+// `.bubbles`, `.timeStamp`, …) on the resulting plain object invokes the
+// native getter with the wrong `this` and throws "Illegal invocation".
+// Vanilla React happens to dodge this because its SyntheticEvent exposes
+// those fields as own data properties; pass-through implementations
+// (preact-style, faiwer-react) do not. Use a Proxy that overrides only
+// target/currentTarget and forwards every other read to the real event
+// with `this = event`, so the host getters run on a real Event.
 function cloneEvent(event, target, value) {
   // A bug report filed on WebKit's Bugzilla tracker, dating back to 2009, specifically addresses the issue of cloneNode() not copying files of <input type="file"> elements.
   // As of the last update, this bug was still marked as "NEW," indicating that it might not have been resolved yet​​.
   // https://bugs.webkit.org/show_bug.cgi?id=28123
   const currentTarget = target.cloneNode(true);
 
-  // click clear icon
-  const newEvent = Object.create(event, {
-    target: {
-      value: currentTarget
-    },
-    currentTarget: {
-      value: currentTarget
-    }
-  });
-
   // Fill data
   currentTarget.value = value;
 
@@ -34,7 +32,18 @@ function cloneEvent(event, target, value) {
   currentTarget.setSelectionRange = (...args) => {
     target.setSelectionRange(...args);
   };
-  return newEvent;
+
+  return new Proxy(event, {
+    get(realEvent, prop) {
+      if (prop === 'target' || prop === 'currentTarget') {
+        return currentTarget;
+      }
+      const v = Reflect.get(realEvent, prop, realEvent);
+      // Bind methods (preventDefault, stopPropagation, …) so they don't
+      // lose `this` when invoked through the proxy.
+      return typeof v === 'function' ? v.bind(realEvent) : v;
+    }
+  });
 }
 export function resolveOnChange(target, e, onChange, targetValue) {
   if (!onChange) {
diff --git a/node_modules/@rc-component/input/lib/utils/commonUtils.js b/node_modules/@rc-component/input/lib/utils/commonUtils.js
index 6e41045..96bd365 100644
--- a/node_modules/@rc-component/input/lib/utils/commonUtils.js
+++ b/node_modules/@rc-component/input/lib/utils/commonUtils.js
@@ -13,23 +13,21 @@ function hasPrefixSuffix(props) {
   return !!(props.prefix || props.suffix || props.allowClear);
 }
 
-// TODO: It's better to use `Proxy` replace the `element.value`. But we still need support IE11.
+// Patched (faiwer): the upstream `Object.create(event, { target, currentTarget })`
+// breaks on real DOM events. Reading any Event.prototype accessor (`.type`,
+// `.bubbles`, `.timeStamp`, …) on the resulting plain object invokes the
+// native getter with the wrong `this` and throws "Illegal invocation".
+// Vanilla React happens to dodge this because its SyntheticEvent exposes
+// those fields as own data properties; pass-through implementations
+// (preact-style, faiwer-react) do not. Use a Proxy that overrides only
+// target/currentTarget and forwards every other read to the real event
+// with `this = event`, so the host getters run on a real Event.
 function cloneEvent(event, target, value) {
   // A bug report filed on WebKit's Bugzilla tracker, dating back to 2009, specifically addresses the issue of cloneNode() not copying files of <input type="file"> elements.
   // As of the last update, this bug was still marked as "NEW," indicating that it might not have been resolved yet​​.
   // https://bugs.webkit.org/show_bug.cgi?id=28123
   const currentTarget = target.cloneNode(true);
 
-  // click clear icon
-  const newEvent = Object.create(event, {
-    target: {
-      value: currentTarget
-    },
-    currentTarget: {
-      value: currentTarget
-    }
-  });
-
   // Fill data
   currentTarget.value = value;
 
@@ -42,7 +40,18 @@ function cloneEvent(event, target, value) {
   currentTarget.setSelectionRange = (...args) => {
     target.setSelectionRange(...args);
   };
-  return newEvent;
+
+  return new Proxy(event, {
+    get(realEvent, prop) {
+      if (prop === 'target' || prop === 'currentTarget') {
+        return currentTarget;
+      }
+      const v = Reflect.get(realEvent, prop, realEvent);
+      // Bind methods (preventDefault, stopPropagation, …) so they don't
+      // lose `this` when invoked through the proxy.
+      return typeof v === 'function' ? v.bind(realEvent) : v;
+    }
+  });
 }
 function resolveOnChange(target, e, onChange, targetValue) {
   if (!onChange) {
