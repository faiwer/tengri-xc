diff --git a/node_modules/@rc-component/motion/es/CSSMotion.js b/node_modules/@rc-component/motion/es/CSSMotion.js
index fc4c468..d566889 100644
--- a/node_modules/@rc-component/motion/es/CSSMotion.js
+++ b/node_modules/@rc-component/motion/es/CSSMotion.js
@@ -28,7 +28,7 @@ export function genCSSMotion(config) {
     return !!(props.motionName && transitionSupport && contextMotion !== false);
   }
   const CSSMotion = /*#__PURE__*/React.forwardRef((props, ref) => {
-    const {
+    let {
       // Default config
       visible = true,
       removeOnLeave = true,
@@ -38,6 +38,12 @@ export function genCSSMotion(config) {
       leavedClassName,
       eventProps
     } = props;
+    // PATCH(@faiwer/react): the @faiwer/react fork normalizes JSX children
+    // into an array even when only a single render-prop function is passed
+    // (the rc-motion / rc-segmented contract is `(motionProps, ref) => node`).
+    // Unwrap so the four `children(...)` call sites below — and the
+    // `isRefNotConsumed(children)` check — see the function rc-motion expects.
+    if (Array.isArray(children)) children = children[0];
     const {
       motion: contextMotion
     } = React.useContext(Context);
diff --git a/node_modules/@rc-component/motion/lib/CSSMotion.js b/node_modules/@rc-component/motion/lib/CSSMotion.js
index 4e8e161..4f258b8 100644
--- a/node_modules/@rc-component/motion/lib/CSSMotion.js
+++ b/node_modules/@rc-component/motion/lib/CSSMotion.js
@@ -40,7 +40,7 @@ function genCSSMotion(config) {
     return !!(props.motionName && transitionSupport && contextMotion !== false);
   }
   const CSSMotion = /*#__PURE__*/React.forwardRef((props, ref) => {
-    const {
+    let {
       // Default config
       visible = true,
       removeOnLeave = true,
@@ -50,6 +50,12 @@ function genCSSMotion(config) {
       leavedClassName,
       eventProps
     } = props;
+    // PATCH(@faiwer/react): the @faiwer/react fork normalizes JSX children
+    // into an array even when only a single render-prop function is passed
+    // (the rc-motion / rc-segmented contract is `(motionProps, ref) => node`).
+    // Unwrap so the four `children(...)` call sites below — and the
+    // `isRefNotConsumed(children)` check — see the function rc-motion expects.
+    if (Array.isArray(children)) children = children[0];
     const {
       motion: contextMotion
     } = React.useContext(_context.Context);
